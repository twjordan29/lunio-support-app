# Lunio Support

Mobile support app for Lunio support staff.

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start --clear
   ```

The app uses the Lunio launcher icon and splash assets from `assets/images`.
